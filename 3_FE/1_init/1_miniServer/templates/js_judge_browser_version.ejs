<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>判断浏览器版本</title>
    <link rel="stylesheet" href="/resources/css/popup.css">
</head>
<body>
    
    <!-- 提示用户升级浏览器 (start) -->
    <div class="popup popup-browser" style="display:block">
        <div class="popup-in">
            <div class="popup-cont">
                <p><img src="/resources/images/pic_popup_browser_900x550.jpg" alt=""></p>
                <a class="btn-link-close" href="#"></a>
                <a class="btn-link-download" href="http://windows.microsoft.com/zh-cn/internet-explorer/download-ie" target="_blank"></a>
            </div>
        </div>
        <div class="popup-bg"></div>
    </div>
    <!-- 提示用户升级浏览器 (end) -->
    
    <script type="text/javascript">
        /* 判断浏览器版本 */ 
        // judgeBrowser();
        /* 判断ie版本 */ 
        judgeBrowserIe();

        /* 判断浏览器版本 */ 
        function judgeBrowser(){
            var Sys={};
            var ua=navigator.userAgent.toLowerCase();
            var s;
            (s=ua.match(/msie ([\d.]+)/))?Sys.ie=s[1]:
            (s=ua.match(/firefox\/([\d.]+)/))?Sys.firefox=s[1]:
            (s=ua.match(/chrome\/([\d.]+)/))?Sys.chrome=s[1]:
            (s=ua.match(/opera.([\d.]+)/))?Sys.opera=s[1]:
            (s=ua.match(/version\/([\d.]+).*safari/))?Sys.safari=s[1]:0;
            if(Sys.ie){//Js判断为IE浏览器
                alert('http://www.phpernote.com'+Sys.ie);
                if(Sys.ie=='9.0'){//Js判断为IE 9
                }else if(Sys.ie=='8.0'){//Js判断为IE 8
                }else{
                }
            }
            if(Sys.firefox){//Js判断为火狐(firefox)浏览器
                alert('http://www.phpernote.com'+Sys.firefox);
            }
            if(Sys.chrome){//Js判断为谷歌chrome浏览器
                alert('http://www.phpernote.com'+Sys.chrome);
            }
            if(Sys.opera){//Js判断为opera浏览器
                alert('http://www.phpernote.com'+Sys.opera);
            }
            if(Sys.safari){//Js判断为苹果safari浏览器
                alert('http://www.phpernote.com'+Sys.safari);
            }
        }
        /* 判断ie版本 */ 
        function judgeBrowserIe() {
            var sys = {}
            var version, ua, ie;
            ua = navigator.userAgent.toLowerCase();
            ie = ua.match(/msie/);
            if (ie) {
                version = ua.match(/msie ([\d.]+)/);
                version = parseInt(version[1], 10);
                version ? sys.ie = version : null;
                if(version < 9){
                    // to do something
                }
            }
        }
    </script>
</body>
</html>